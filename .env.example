# ─── Credit Scoring Strategy — окружение ───────────────
# Скопируйте в .env и заполните при необходимости.
# Пока проект не использует переменные окружения — это
# контракт на будущее (БД, API-ключи и т.д.).

# DEBUG=1
# DATABASE_URL=sqlite:///app.db
